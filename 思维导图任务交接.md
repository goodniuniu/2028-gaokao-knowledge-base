# 🧭 思维导图任务交接文档

> **交接时间**：2026-08-21
> **交接对象**：下一个 AI 会话（先读 `AGENTS.md`，再读本文件）
> **新任务目标（用户原话）**：增加思维导图，能从知识点的细节中找到关联关系
> **本文件性质**：一次性任务交接，任务完成后可归档删除

---

## 一、仓库现状速览（2026-08-21，全部已推送）

- **知识卡片 262 张**：语文45 / 数学63 / 英语41 / 物理33 / 化学36 / 生物33 / 方法11（以 `python kb_audit.py` 现场输出为准）
- **站点视觉 v2**：编辑部纸感风格（暖纸底/发丝线/墨蓝主色），`assets/kb.css` 全量重写
- **首页**：由 `build_site.py` 的 `build_root_index()` 模板生成（⚠️ `index.md` 已删除，首页不再由 md 驱动；hero统计数字现场统计，勿手工维护）
- **女儿专区**：`04_给女儿的使用说明` + `05_能量补给站` 两个文档，治愈系主题由三层机制实现（`GIRL_ZONE_WIDGETS` 注入 + `kb.css` 末尾 girl-zone 段 + `kb-progress.js` 的 `Boost` 模块：每日一句/连续天数/成就徽章）。**改这两页只需改 md**；新增同类页面要在 `ROOT_DOCS` + `GIRL_ZONE_WIDGETS` 各加一条
- **每用户进度系统**：卡片页三键标记（❌⚠️✅）+ Cloudflare Worker 云同步（`worker/`，线上 `gaokao-kb-sync.goodniuniu.workers.dev`）。**cardId = md 路径去 .md 后缀，卡片改名/移动会使用户进度失联**
- **线上地址**：`https://www.goodniuniu.com/2028-gaokao-knowledge-base/`（github.io 会 301 过来）

### 本次交接前的最近提交（供回溯）

| commit | 内容 |
|--------|------|
| `f87a2e6` | 整站视觉 v2 重设计 + 首页编辑部式重排 + 删除僵尸 index.md |
| `33fd2f5` | 女儿专区：05能量补给站 + 治愈系视觉 + 每日一句/成就徽章 |
| `138a420` | AGENTS.md 固化多会话协作规则（开工先 fetch） |
| `f9613dc` | 本地147快照与远程260卡快照大合并（262张） |

---

## 二、新任务：思维导图——已核实的数据资产

**核心发现：关联关系数据已经存在，无需从零标注。**

1. **「关联卡片」互链网络（人工维护，质量高）**：
   - 全部 262 张卡片都有 `## 关联卡片` 段，**共 1422 条 `[](xxx.md)` 链接**
   - 分科：数学263 / 语文236 / 英语234 / 化学229 / 生物198 / 物理196 / 方法66
   - 链接位于卡片 md 的「关联卡片」段，格式 `- [卡片名](相对路径.md) — 关联说明`；另一会话已用 `check_backlinks.py` 修到 0 断链、双向化（1127→1405 条），互链基本可靠
2. **`assets/kb-cards.js`**：build_site 生成的卡片清单，格式 `window.KB_CARDS = { "<cardId>": {"d": "维度", "s": "科目"}, ... }`，可直接作图数据源
3. **每卡元信息表**：状态（✅⚠️❌）/ 难度（★）/ 时间标签（高一筑基·高二深化·高三冲刺）/ 试卷来源（#新高考Ⅰ卷·广东 等）/ 广东考情——都可作为导图的分组维度或节点着色
4. **卡片尾部 `🏷️ 标签` 行**：hashtags（如 `#语文 #素材与拓展 #广东特色 #张桂梅`），可解析出来做标签聚合视图
5. **`check_backlinks.py`**：检测断链/路径不规范/单向引用（写 `复盘追踪/_backlinks_report.txt`，当前未生成，运行即可获得全量边表）

## 三、方案建议（供选择，不锁死）

任务目标拆成两层，建议都做：

- **A. 结构性思维导图（每科一棵知识树）**：科目 → 四维度 → 卡片 → 卡内知识点。静态生成优先推荐 **markmap**（markdown→可折叠交互导图，CDN 引入即可，符合纯静态架构）；也可 mermaid mindmap
- **B. 关联网络图谱（全库/分科）**：以卡片为节点、1422 条互链为边，用力导向图（ECharts graph / d3-force）渲染；节点按科目着色、按维度分形状，点节点跳转卡片页。"从细节找关联"的增强边：共同试卷来源、共同标签、跨科互链（如物理↔数学）
- **C. 零依赖备选**：build 时用 Python（networkx + graphviz 或手写）生成静态 SVG，无 JS 依赖但无交互

**落位建议**：新建 `思维导图/` 专区（类比 `图形库/`），生成脚本 `思维导图/build_mindmaps.py` 或扩展 build_site.py；首页「方法与工具」区加一张入口卡；每科索引页顶部可嵌本科小图。注意 CDN 方案在无网环境会退化，需留纯文本降级。

## 四、工程约束（必守，详见 AGENTS.md）

1. **开工先 `git fetch`** 对比 origin/main，有分叉先合并（本仓库多会话并行，7-8月曾分叉17个提交）
2. **MD 唯一源**：卡片页由 md 重建，不手工编辑任何生成 HTML
3. 改任何 md 后必跑 `python build_site.py` → "链接校验通过：无断链"；大量改动后跑 `python check_html_md_consistency.py`（应 0 不一致）
4. 统计数字唯一来源 `python kb_audit.py`，只在 `00_知识库总索引.md` 展示
5. 移动端适配必须有（主要用户在手机上）；不要破坏 girl-zone 主题与进度组件
6. `commit/push` 习惯：本仓库近期模式为"交付即推送"（用户接受），但涉及删除/重命名卡片 md 时必须先告知用户进度失联风险
7. 新增根目录 md 若要进站点导航，需同时加 `ROOT_DOCS` + `ROOT_DOC_ICONS`（本交接文档故意不加，md-only）

## 五、三闸门验收标准（任务完成定义）

- [ ] `python build_site.py` 无断链
- [ ] `python check_html_md_consistency.py` 0 不一致
- [ ] `python kb_audit.py` 262 张 0 缺 HTML（卡片数不变，导图是视图不是新卡片）
- [ ] 手机视口（≤640px）下导图可用/可读
- [ ] 思维导图入口出现在首页，且女儿专区、三键标记、每日一句功能回归正常

---

> 📌 补充：用户视角的深层诉求——女儿尚未开始使用知识库，一切功能优先"低摩擦、随需取用"。思维导图若做成她复习时可一眼看清知识结构的工具（而非维护者的花架子），价值最大。建议至少做一版"考前扫一眼"的每科速览树。
