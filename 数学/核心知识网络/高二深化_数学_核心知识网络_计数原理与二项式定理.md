# 计数原理与二项式定理

| 字段 | 内容 |
|------|------|
| **来源** | 53科学备考《高中知识清单》数学知识图谱 / 人教A版选择性必修第三册第六章 |
| **时间标签** | #高二深化 |
| **难度** | ★★★☆☆ |
| **状态** | ⚠️待强化 |
| **试卷来源** | #新高考Ⅰ卷·广东 |
| **广东考情** | 考查频率：中频；难度：中档；排列组合与二项式定理是选填常考点，注意分类讨论和排除法的运用 |

---

## 核心内容

### 一、排列与组合
- **排列**：从 $n$ 个不同元素中取出 $m$（$m \leq n$）个元素，按一定顺序排成一列
- **排列数**：$A_n^m = n(n-1)(n-2)\cdots(n-m+1) = \frac{n!}{(n-m)!}$（$m, n \in \mathbb{N}^*, m \leq n$）
- **组合**：从 $n$ 个不同元素中取出 $m$（$m \leq n$）个元素作为一组
- **组合数**：$C_n^m = \frac{A_n^m}{A_m^m} = \frac{n(n-1)(n-2)\cdots(n-m+1)}{m!} = \frac{n!}{m!(n-m)!}$（$m, n \in \mathbb{N}^*, m \leq n$）

### 二、组合数性质
- $C_n^m = C_n^{n-m}$（对称性）
- $C_{n+1}^m = C_n^m + C_n^{m-1}$（递推公式）

### 三、二项式定理
$$(a + b)^n = C_n^0 a^n + C_n^1 a^{n-1}b + C_n^2 a^{n-2}b^2 + \cdots + C_n^k a^{n-k}b^k + \cdots + C_n^n b^n$$

- **通项公式**：$T_{k+1} = C_n^k a^{n-k} b^k$（$k = 0, 1, 2, \dots, n$）
- **二项式系数**：第 $k+1$ 项的二项式系数为 $C_n^k$

### 四、二项式系数的性质
- **对称性**：$C_n^k = C_n^{n-k}$
- **增减性与最大值**：
  - $n$ 为偶数时，中间一项 $C_n^{\frac{n}{2}}$ 最大
  - $n$ 为奇数时，中间两项 $C_n^{\frac{n-1}{2}}$ 和 $C_n^{\frac{n+1}{2}}$ 相等且最大
- **各二项式系数和**：$C_n^0 + C_n^1 + C_n^2 + \cdots + C_n^n = 2^n$
- **奇数项与偶数项的二项式系数和相等**：$C_n^0 + C_n^2 + C_n^4 + \cdots = C_n^1 + C_n^3 + C_n^5 + \cdots = 2^{n-1}$

---

## 题型识别标志

> **看到什么条件 → 立刻想到什么方法**

| 题干关键条件 | 识别为 | 首选方法 |
|------|------|------|
| 求二项式展开特定项系数 | 通项公式 | $T_{k+1}=C_n^k a^{n-k}b^k$ |
| 乘积形式（如 $(1-\frac yx)(x+y)^8$） | 分项合并 | 逐项匹配目标幂次后相加 |
| 求系数和 | 赋值法 | 令 $x=1$ 或 $x=-1$ |
| 排列组合计数 | 基础方法 | 优先法/捆绑/插空/排除 |
| "二项式系数最大" | 对称性 | 中间项（奇偶有别） |
| 区分概念 | 易混点 | "二项式系数" $C_n^k$ ≠ "项的系数" |

## 解题路径（特定项系数四步）

### 第一步：写通项
$$(x+y)^n:\quad T_{k+1}=C_n^k x^{n-k}y^k,$$
明确 $k$ 对应 $y$ 的幂次。

### 第二步：分项匹配
对乘积式，分别令每一项（如 $1$ 与 $-\frac yx$）产生目标幂次。

### 第三步：解方程定 $k$
令总指数等于目标，解出 $k$，算出对应系数（注意符号）。

### 第四步：合并得结果
各项系数相加。

> **易错提醒**：混淆"二项式系数"与"项的系数"；乘积展开时漏掉负号的分配；通项中 $k$ 的计数基准弄反。

## 母题（2022 新课标Ⅰ卷·第13题，5分）

**题目**：$(1-\frac{y}{x})(x+y)^8$ 的展开式中 $x^2y^6$ 的系数为 ________。（用数字作答）

**解答**：
$$(x+y)^8=\sum_{k=0}^8 C_8^k x^{8-k}y^k.$$
- 来自 $1\cdot(x+y)^8$：需 $x^2y^6$，即 $8-k=2\Rightarrow k=6$，系数为 $C_8^6=28$。
- 来自 $-\frac yx\cdot(x+y)^8$：$-\frac yx\cdot C_8^k x^{8-k}y^k=-C_8^k x^{7-k}y^{k+1}$，需 $x^2y^6$，即 $7-k=2\Rightarrow k=5$，系数为 $-C_8^5=-56$。

合计系数：$28+(-56)=-28$。

故填空为 **$-28$**。

---

## 关联卡片

- [条件概率与离散型随机变量](高二深化_数学_核心知识网络_条件概率与离散型随机变量.md) — 计数原理是概率计算的基础

---

## 备注
- 排列组合解题策略：优先法、捆绑法、插空法、定序问题、分组分配问题、正难则反（排除法）
- 二项式展开中注意区分"二项式系数"和"项的系数"
- 广东卷常考：二项式展开特定项系数、赋值法求系数和
