{
  "name": "gaokao-kb-sync",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for 2028-gaokao-knowledge-base per-user progress sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
