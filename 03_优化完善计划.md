# 🔧 知识库优化完善计划（工程 + 内容）

> **制定时间**：2026年7月20日（基于全库审计与工程调研）
> **定位**：本文件是 `00_知识库完善计划（650分目标）.md` 的执行层补充——650分计划回答"补什么内容"，本计划回答"按什么顺序、用什么机制、如何交接执行"。
> **交接说明**：后续完善工作由 AI 助手按本计划逐阶段执行；开始前请先阅读本文件与 `AGENTS.md`（阶段0第1步创建）。

---

## 一、现状审计结论（2026-07-20）

### 1. 内容结构

卡片总数 **92 张**（六科+方法口径，英语单词复习系统独立统计），卡片本身质量高（元信息表 + 干货提炼 + LaTeX 公式，符合 `01_知识卡片模板.md`），但结构失衡明显：

| 维度 | 现状 | 缺口 |
|------|------|------|
| **学科失衡** | 数学36 / 物理15 / 语文11 / 化学11 / 生物11 / 方法6 / **英语仅2张** | 英语是最严重短板（另有独立单词复习系统：77个 Day 词表 + 小测卷 + 生成脚本） |
| **易错警示** | 全库仅5张（数学2、物理1） | 语/英/化/生四科为 **0** |
| **素材与拓展** | 语文2、数学1 | 英/物/化/生为 **0** |
| **掌握状态** | 0 已掌握 / 91 待强化 / 1 未理解 | 复盘机制尚未真正运转，掌握度未破零 |
| **与650分计划差距** | 第一阶段目标110+张 | 现有92张且结构偏离计划清单 |

### 2. 工程现状

工具链可用：`build_site.py`（一键重建：卡片换肤 + 索引生成 + 断链校验）、`kb_audit.py`（全库审计）、`build_status_table.py`（复盘状态表）。存在以下技术债：

1. **无 `.gitignore`**：生成物（如 `复盘追踪/_scan_result.json`）入库。
2. **脚本债务**：`generate_index.py` 已被 build_site.py 取代（文件头自注明），且内含旧机器绝对路径 `C:/Users/user/WPSDrive/...`；`draw_batch2.py` 在根目录与 `图形库/` 各一份且**内容不一致**；`fix_management_docs.py`、`process_images.py` 为一次性脚本，无用途说明。
3. **索引漂移**：`数学/索引_数学.md` 写"素材与拓展 2 张"实际 1 张，索引更新时间停留在"2025年7月"；其他科索引大概率同样失准。
4. **HTML 脱节**：`英语/单词复习/分组词表/` 77 个 `Day_*.md` 无对应 HTML；`广东高考知识库指令.md` 无 HTML（需确认口径：有意保留 md-only 还是遗漏）。
5. **无接手入口**：没有 `AGENTS.md`/`README`，新接手的 AI 无法快速得知构建流程、脚本职责、命名规范。
6. **无依赖声明**（画图脚本依赖 matplotlib 等，无 requirements.txt）、无 CI。
7. `index.md` 与 `00_知识库总索引.md` 内容职责重叠，易再漂移。

---

## 二、阶段0：工程治理（低风险，最先做）

> 目标：让仓库"可交接、可重建、数字可信"。预计一个工作会话内完成。

- [x] **0.1 新建 `AGENTS.md`**（接手入口，最重要）：项目结构说明、卡片模板与命名规范（指向 `01_知识卡片模板.md`）、`build_site.py` / `kb_audit.py` / `build_status_table.py` 各自职责与执行顺序、画图脚本位置（`图形库/`）、提交前检查清单：**改 md → 跑 `python build_site.py` → 跑 `python kb_audit.py` 更新统计数字 → 再提交**。
- [x] **0.2 新建 `.gitignore`**：排除 `复盘追踪/_scan_result.json`、`__pycache__/`、`*.pyc` 及临时产物；77 个 Day 词表 md 是内容源，保留入库。
- [x] **0.3 清理脚本**：删除或归档 `generate_index.py`（已被取代，建议移入 `_archive/`）；`draw_batch2.py` 只保留一份（以 `图形库/` 内为准，对比后取舍）；给 `fix_management_docs.py`、`process_images.py`、`英语/单词复习/*.py` 补一行用途头注或移入 `_archive/`。
- [x] **0.4 修复索引漂移**：以 `python kb_audit.py` 输出为准，重算各科 `索引_*.md` 的卡片数与更新时间；明确 `index.md` 与 `00_知识库总索引.md` 职责——`index.md` 仅做门户导航，统计数字全库只保留一份（在总索引，由审计数据驱动更新）。
- [x] **0.5 补 `requirements.txt`**：按画图脚本实际 import 生成（matplotlib 等）。
- [x] **0.6 验证**：`python build_site.py` 全量重建 → 断链清单为空 → `git status` 无意外变更。

## 三、阶段1：内容结构补全（按650分计划第一阶段清单）

> 目标：对接 `00_知识库完善计划（650分目标）.md` 第一阶段（2026.9 前 110+ 张），优先补最失衡处。新增卡片必须严格走 `01_知识卡片模板.md`：元信息表 + 广东考情标注 + 试卷来源标签 + 关联卡片。

优先级排序（缺口 × 分值权重）：

- [x] **1.1 英语（最严重短板，仅2张）**：核心知识网络6张（时态语态总表、三大从句连词、非谓语对比、特殊句式、词根词缀、高级句型）、典型题型6张（阅读四题型通法、七选五、完形语境线索、语法填空口诀、应用文模板、读后续写情节构建）、易错4张、素材4张；**广东听说专区起步**（模仿朗读/三问五答/故事复述各1张）。✅ 2026-07-20 完成：新增22张，英语卡片 2→24 张，全库 92→114 张。
- [x] **1.2 易错警示维度全科启动**：语/英/化/生各补4张（清单见650分计划第一阶段：成语误用、实词一词多义、病句、作文偏题；易混动词、介词搭配、时态语境、从句辨析；阿伏伽德罗常数陷阱、离子少量过量；光反应暗反应、自交vs自由交配）。✅ 2026-07-20 完成：英语4张随1.1建成；语文4张（+作文偏题）、化学4张（+等效平衡、电化学阴阳极）、生物4张（+交叉互换vs易位、神经递质vs激素），全库 114→126 张，易错维度四科全部破零。
- [ ] **1.3 素材与拓展维度**：物理（华为5G/比亚迪/大疆/港珠澳大桥）、化学（茂名石化/珠海盐化工/粤北稀土）、生物（生物医药/农业育种/海洋生态）各1-2张。
- [ ] **1.4 物化生核心知识网络对照勾选**：对照650分计划第一阶段清单，核对电场/磁场公式体系、化学计量、元素周期律、细胞结构、代谢体系等是否已入库（部分已完成），补缺漏。
- [ ] **1.5 每批新卡完成后**：更新对应 `索引_*.md`、跑 `kb_audit.py` 校准总索引统计、跑 `build_site.py` 重建站点。

## 四、阶段2：复盘机制运转

- [ ] **2.1 首次正式复盘**：用 `build_status_table.py` + `kb_audit.py` 梳理全部92张卡片，按实际掌握情况重新标记，掌握状态"破零"（目标10+张 ✅）。
- [ ] **2.2 固化周复盘流程**：错题四分类（知识性/方法性/审题性/计算性）→ 匹配或新建卡片 → 更新 `复盘追踪/知识掌握状态表.md` → `build_site.py` 重建。
- [ ] **2.3 使用场景提示词化**：把 `广东高考知识库指令.md` 第三节的"周复盘/考前冲刺20点/广东卷趋势分析"落成可直接复用的提示词模板（放入 `02_使用规则与检索说明.md` 或 AGENTS.md）。

## 五、阶段3：可选增强（高三前完成即可）

- [ ] **3.1 GitHub Actions CI**：push 时自动跑 `build_site.py` + 断链校验，防止 HTML 与 md 再脱节。
- [ ] **3.2 Day 词表 HTML 口径**：补齐 `英语/单词复习/分组词表/` 77 个词表的 HTML，或在 AGENTS.md 中明确声明该目录 md-only。
- [ ] **3.3 站点搜索/标签聚合页**：静态生成，按 `#标签`（时间轴/难度/试卷来源/广东特色）聚合卡片。
- [ ] **3.4 图形库扩充**：继续按科目补示意图并嵌入卡片（现有32张：物理/化学/生物/数学各8张）。

---

## 六、执行规则（给接手的 AI）

1. **先做阶段0再做内容**：AGENTS.md 与 .gitignore 是所有后续工作的地基。
2. **改内容必改三处**：新卡片 md → 对应 `索引_*.md` → `kb_audit.py` 校准总索引 → `build_site.py` 重建 HTML。
3. **内容口径以两个文件为准**：`广东高考知识库指令.md`（收录规则与广东考情）+ `01_知识卡片模板.md`（格式规范）。
4. **数量目标以 `00_知识库完善计划（650分目标）.md` 第六节"关键成功指标"为准**，本计划不另立指标。
5. 每完成一个阶段，更新本文件的复选框与 `00_当前状态记录.md`。
