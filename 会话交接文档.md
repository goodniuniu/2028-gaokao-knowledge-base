# 📦 会话交接文档（2026-08-21）

> **交接对象**：下一个 AI 会话。先读 `AGENTS.md`（工程规则）→ 再读本文件（会话状态与开放线索）
> **性质**：会话级交接，下次会话开工时可按"开放线索"接续；被新交接替代时可归档删除
> **前一版**：`思维导图任务交接.md`（已完成并删除，记录见 `00_当前状态记录.md` 2026-08-21 条目）

---

## 一、仓库现状快照（2026-08-21 深夜，全部已推送）

| 维度 | 状态 |
|------|------|
| 知识卡片 | **262 张**（语文45/数学63/英语41/物理33/化学36/生物33/方法11；`kb_audit.py` 为唯一权威） |
| 站点视觉 | **v2 编辑部纸感**（暖纸底/发丝线/墨蓝；首页由 `build_root_index()` 模板生成，index.md 已删除） |
| 思维导图 | hub + 7棵知识树 + **考前速览树（98真题锚定关键卡，英语回退口径）** + 关联图谱（1422实线互链 + **33条隐性关联虚线**：同标签/同卷跨科）+ **六科索引页嵌迷你树**；echarts 已本地化 |
| 女儿专区 | 04使用说明（三句咒语+考前速览入口）+ 05能量补给站（6个真实低谷故事）；治愈系主题 + Boost 模块（每日一句/连续天数/8枚成就徽章，未使用时隐藏） |
| 进度系统 | 网页三键标记 + Cloudflare Worker 云同步（cardId=md路径，**卡片改名=进度失联**） |
| 示意图 | 33 张（生物9，新增基因表达载体与双酶切）；首页图形库计数已改动态统计 |
| 线上地址 | `https://www.goodniuniu.com/2028-gaokao-knowledge-base/` |

**本会话提交**（2026-08-20~21，均已在远程）：`f9613dc`合并262卡 → `138a420`多会话规则 → `33fd2f5`女儿专区 → `f87a2e6`整站v2 → `41a00ce`思维导图交接 → `8aa297d`思维导图专区 → `73d0a12`基因工程卡升级 → `f80016e`思维导图三增强

## 二、本会话关键决策记录（避免下次翻案）

1. **外部AI评审过滤模式**：用户请其他AI评审卡片 → 我按「广东考纲边界 + 仓库卡片分工 + 当前瓶颈」三维过滤后落地。已拒绝项（PCR引物设计细则/蓝白斑/测序细节=超纲；导入方法表/T-DNA=底座卡已有）记录在 `00_当前状态记录.md` 2026-08-21 基因工程条目，**勿再重复添加**。
2. **内容原则**：用户明确要求"让人喜欢阅读，而非有干货就受欢迎"——所有卡片补充须配口诀/类比/图示；"能用图就不要省"。
3. **隐性边限流参数**（在 `collect_map_data()`）：标签仅2-4张卡、跨科优先、星形连法、标签边≤36/真题边≤12/总量≤48；与实线互链去重。
4. **速览树口径**：关键卡=来源含"20XX年…T题号/第N题"；无锚定科目回退"真题来源+高频"前15张（当前仅英语用回退，后续英语卡补题号锚定后自动切换）。

## 三、开放线索（下次会话接续优先级）

1. **⭐ 首次复盘（最高优先，等女儿配合）**：全库掌握状态仍 0 张✅。启动方式已固化——用户对 AI 说「启动首次全库复盘」（`02_使用规则与检索说明.md` 第四节），或女儿在网页自助点三键。目标 2026.9 高二开学前 ✅≥10 张。
2. **开学后节奏（2026.9）**：第一次月考后走「模板四」错题四分类入库；弱科卡片可用"外部AI评审→过滤→可读性落地"模式升级（每次只做当月最弱科目2-3张，勿全科铺开）。
3. **工程待办**：阶段3.1 GitHub Actions CI（push自动build+三闸门）；阶段3.3 标签聚合页；英语卡片来源补题号锚定（使速览树脱离回退口径）。
4. **内容待办**：图形库第三批（晶胞/食物链网/光路图/立体截面）；650分计划第二阶段（高二下2027.2 题型深化，勿提前堆）。

## 四、本会话已知环境问题

- **本会话环境的图片CDN上传通道损坏**：Read 工具读 PNG 返回的 URL 内嵌本地路径（无效）。视觉验证已用「浏览器 DOM 断言 + 功能断言」替代（canvas存在/数据加载/点击跳转）。下次会话可重试是否恢复。
- 测试用本地服务器（localhost:8631）已停止；用户浏览器残留 2 个本地标签页属正常。
- Windows Git Bash 控制台显示中文会乱码（文件本身 UTF-8 正常），排查时用 Python 读文件而非 cat/grep 中文输出。

## 五、速查：三闸门与红线

```
python build_site.py                  # 必须"链接校验通过：无断链"
python check_html_md_consistency.py   # 必须 0 不一致
python kb_audit.py                    # 262 张 0 缺HTML（数字唯一权威）
```

红线：开工先 `git fetch` 有分叉先合并 ｜ MD唯一源不手改生成HTML ｜ echarts 用本地 `assets/echarts.min.js` 勿改CDN ｜ 卡片md改名会使用户网页进度失联 ｜ commit/push 按近期"交付即推送"惯例，但删改卡片文件前必须告知用户
